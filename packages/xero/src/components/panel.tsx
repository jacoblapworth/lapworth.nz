import { styled } from '@/styled/jsx'

export const Panel = styled('div', {
  base: {
    backgroundColor: 'background.primary',
    border: 'subtle',
    borderRadius: 'md',
    padding: 'md',
  },
})
