import { styled } from '@/styled/jsx'

export const Article = styled('article', {
  base: {
    alignItems: 'start',
    display: 'flex',
    flexDirection: 'column',
    gap: 'md',
    marginBlock: 'xl',
  },
})
