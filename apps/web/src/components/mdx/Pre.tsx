import { styled } from '@/styled/jsx'

export const Pre = styled('pre', {
  base: {
    // border: 'muted',
    borderRadius: 'lg',
    marginBlockEnd: 'lg',
    overflowX: 'auto',
    padding: 'md',
  },
})
