import { styled } from '@/styled/jsx'

export const Pre = styled('pre', {
  base: {
    // backgroundColor: 'surface',
    // borderRadius: 'md',
    border: 'muted',
    overflowX: 'scroll',
    paddingBlock: 'md',
  },
})
