import { styled } from '@/styled/jsx'

export const Section = styled('section', {
  base: {
    display: 'flex',
    flexDirection: 'column',
    gap: 'md',
    maxWidth: '800px',
    width: '100%',
  },
})
