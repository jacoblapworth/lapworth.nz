import { styled } from '@/styled/jsx'

export const Wrapper = styled('div', {
  base: {
    display: 'grid',
    gridAutoColumns: 'auto',
    gridAutoFlow: 'row',
    marginBlockEnd: 'lg',
    position: 'relative',
    rowGap: 'lg',
    width: '100%',
  },
})
