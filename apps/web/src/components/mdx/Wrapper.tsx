import { styled } from '@/styled/jsx'

export const Wrapper = styled('div', {
  base: {
    display: 'grid',
    gridAutoColumns: 'auto',
    gridAutoFlow: 'row',
    width: '100%',
    // marginBlockEnd: 'lg',
    // maxWidth: 1000,
    // position: 'relative',
  },
})
