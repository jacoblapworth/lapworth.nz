{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["tokens", "content", "video", "^build"],
      "outputs": [".next/**", ".vercel/**", "!.next/cache/**"]
    },
    "content": {
      "dependsOn": ["^content"],
      "inputs": ["src/content/**", "velite.config.ts"],
      "outputs": [".velite/**", "public/static/**"]
    },
    "content:watch": {
      "dependsOn": ["^content:watch"],
      "inputs": ["src/content/**", "velite.config.ts"],
      "outputs": [".velite/**", "public/static/**"],
      "persistent": true
    },
    "dev": {
      "with": ["content:watch", "video:watch"]
    },
    "tokens": {
      "dependsOn": ["^tokens"],
      "outputs": [".styled/**"]
    },
    "video": {
      "dependsOn": ["^video"],
      "inputs": ["src/videos/**"],
      "outputs": ["src/videos/**"],
      "persistent": true
    },
    "video:watch": {
      "dependsOn": ["^video:watch"],
      "inputs": ["src/videos/**"],
      "outputs": ["src/videos/**"],
      "persistent": true
    }
  }
}
