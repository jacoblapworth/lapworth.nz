{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "cache": false,
      "dependsOn": ["^dev", "typegen"],
      "persistent": true
    },
    "typegen": {
      "dependsOn": ["^typegen"],
      "inputs": ["$TURBO_DEFAULT$", "wrangler.jsonc"],
      "outputs": ["worker-configuration.d.ts", ".wrangler/**"]
    }
  }
}
